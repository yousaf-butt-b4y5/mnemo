# Mnemo — never commit data, secrets, or stores
data/
store/
backups/
.claude/
*.db
*.db-shm
*.db-wal
*.db-journal
*.db-wal
.env
__pycache__/
*.pyc
dist/
build/
*.spec

# Local-only utility/migration scripts — never publish (private context)
tools/
